# A small Shiny app for exploring gene networks

- Requires `igraph` and `ggnetwork` (which requires `ggplot2` and `intergraph`)
- User can save the current network view as a static image, and node attributes as a .csv

